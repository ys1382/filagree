import 'ui'

chat = []


sys.ui(chat,
        ['vertical',
            ['label', 'text':'abi'],
            ['input', 'name':'record', 'hfill':true, 'readonly':true],
            ['input', 'name':'send'],
            'w':240, 'h':320
        ] )


sys.print(chat.ui)

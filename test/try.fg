/*
p = 'y'
q = 'z'
a = ['b', p, q : 'm']
a.remove(0,a.length)
sys.print(a)
*/

import 'ui'

chat = []

sys.ui(chat,
        ['vertical',
            ['label', 'text':'abi'],
            ['input', 'name':'record', 'fill':true, 'readonly':true],
            ['input', 'name':'send'],
            'w':240, 'h':320
        ] )

sys.print(chat.ui)

sys.loop()

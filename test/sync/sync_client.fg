####### sync_client.fg


import 'sync'
import '../ui'
import '../mesh'


client = [

    'select' : function(client)
        sys.print('select ' + sys.args()[1])
    end,

]

client.ui = sys.ui( client,
                    ['vertical',
                        ['table', 'name':'files', 'list':['x','y'], 'logic':client.select],
                        'w':240, 'h':320
                    ])

mesh = Mesh.init('client', 9999, [client])
sync = Sync.init(client, ['client'], mesh)

mesh.connect()
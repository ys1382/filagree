import 'sync'

test_director = [

    'connected' : function(self, peer)

        msg1 = ['event' : 'test',
                'id' : 'xyz',
                'directive' : 'self.test_file_update(\'x.txt\', \'y\')']
        sys.send(peer, msg1)
    end
]

sys.connect('127.0.0.1', port, test_director) # client connects to server
sys.loop()

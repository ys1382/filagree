####### sync_server.fg

id = 'server'
port = 9999

import '../mesh'                    # shared mesh logic
import 'sync'                       # shared sync logic

mesh = Mesh.init(nil, id, port)
sync = Sync.init(nil, mesh)

mesh.listen()
sys.loop()